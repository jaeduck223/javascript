//조건 연산자(피연산자를 3개 가짐) 실습
//조건 연산자 -> 간단한 코드 작성 가능!
let test;

test = typeof(test) != 'undefined' ? test : "초기화_1";
console.log(test);

test = typeof(test) != 'undefined' ? test : "초기화_2";
console.log(test);